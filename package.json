{
  "name": "hexo-blockquote2note",
  "version": "1.0.0",
  "description": "A Hexo plugin that renders blockquotes to note tag in NexT theme.",
  "main": "dist/index.js",
  "scripts": {
    "prepublish ": "npm install && npm run clean && npm run build",
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "eslint": "eslint .",
    "pretest": "npm run clean && npm run build",
    "test": "mocha -r ts-node/register 'test/**/*.ts'"
  },
  "directories": {
    "lib": "./lib"
  },
  "files": [
    "dist/**"
  ],
  "types": "./dist/index.d.ts",
  "repository": "BlockLune/hexo-blockquote2note",
  "homepage": "https://github.com/BlockLune/hexo-blockquote2note",
  "keywords": [
    "hexo",
    "next-theme"
  ],
  "author": "BlockLune",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "chai": "^5.0.3",
    "eslint": "^8.56.0",
    "eslint-config-hexo": "^5.0.0",
    "hexo": "^7.1.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
